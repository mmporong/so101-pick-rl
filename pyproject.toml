[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "so101-pick-rl"
version = "0.1.0"
description = "Cross-simulator SO-101 pick reinforcement learning experiment"
requires-python = ">=3.10"

[tool.setuptools]
packages = []
